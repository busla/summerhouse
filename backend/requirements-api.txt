# Minimal dependencies for API Lambda (OAuth2 callbacks only)
# Keep this lightweight - full agent deps go in AgentCore container
fastapi>=0.115.0
mangum>=0.19.0
pydantic>=2.0.0
boto3>=1.35.0
pyjwt>=2.8.0
